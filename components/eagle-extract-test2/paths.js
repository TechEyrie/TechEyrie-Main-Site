export {
  EAGLE_EXTRACT_PREFIX,
  EAGLE_EXTRACT_ASSET_PATHS,
  EAGLE_EXTRACT_MANIFEST_URL,
  EAGLE_EXTRACT_VALIDATION_URL,
  EAGLE_EXTRACT_COLORS_URL,
} from "../eagle-extract-test1/paths.js";

/** Reference iframe — embed mode matches phase 10 compare. */
export const EAGLE2_REFERENCE_SRC = "/eagle-project-2/?embed=1";

/** Locked hero viewport for pixel parity (phase 9 baseline). */
export const PARITY_VIEWPORT_PX = 720;
