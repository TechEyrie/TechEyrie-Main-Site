import { dark7MainSurfaceStyle } from "../dark7/dark7PageSurface";

/** Dark7-aligned section surface for /services1 listing (matches inner service pages). */
export const services1ListingDarkSurface = dark7MainSurfaceStyle;
